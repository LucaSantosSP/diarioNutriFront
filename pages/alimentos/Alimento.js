import { Text, View } from 'react-native';

export default function Alimento({navigation}) {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Novo Aliemnto!</Text>
      </View>
    );
  }